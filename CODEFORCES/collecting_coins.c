/*#include<stdio.h>

int main()
{
    unsigned long long int a, b, c, d, t, i, res;

    scanf("%lld",&t);

    for(i = 0; i < t; i++)
    {
        scanf("%lld %lld %lld %lld", &a, &b, &c, &d);
        res = a+b+c+d;
        if(res % 3 == 0)
        {
            if(res%2 == 0) printf("YES\n");
        }
        else printf("NO\n");
    }

    return 0;
}
*/




